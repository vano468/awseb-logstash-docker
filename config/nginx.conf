worker_processes 1;

events {
  worker_connections 1024;
}

http {
  server {
    listen 80;
    server_name localhost;

    location / {
      deny all;
    }

    location /status {
      return 200;
      access_log off;
    }
  }
}
